# 羽球雙打隊伍產生器 — 設計決策文件

本文件記錄 2026-08-05 grilling session 定案的所有設計決策，為 `spec.md` 的補充與細化。實作以本文件為準。

## 產品形態

- 技術棧：Vue 3 + TypeScript + Vite + Tailwind CSS，套件管理用 pnpm
- 純前端、無後端無資料庫；資料以 JSON 存於 localStorage
- 最簡 PWA（manifest + service worker 快取靜態資源），支援離線使用（體育館收訊差為真實場景）
- 部署：Cloudflare Pages，正式網域 `badminton-matcher.creticulata.dev`；部署指令執行前需經使用者確認
- 介面語言：繁體中文
- 主題：操作頁面（名單、記分、歷史、設定）為淺色主題、直式為主；對戰顯示畫面為高對比深底大字
- 螢幕方向：不鎖定方向。對戰畫面橫式為四分割（左二區塊一隊、右二區塊一隊、中間 VS）；直式時改為上下堆疊佈局並顯示「建議轉為橫式」提示

## 資料模型

- **場次（session）制**：全域參賽者名單與 rating 永久保留、跨場次累積；「今日出席名單、上場／休息統計、當日賽程」屬於單一場次，開新場次時重置
- 開新場次時從全域名單勾選今日出席者；中途可加入（遲到）或標記離場（早退）
- CSV：單一檔案匯出／匯入，內含參賽者與比賽紀錄兩個區段（如 `[players]` / `[matches]`）；匯入為**覆蓋還原**語意（換手機／備份用途），匯入前跳確認

## 賽程與分組

- 單場地：一回合一場比賽（架構保留未來多場地擴充，第一版不做 UI）
- **逐回合產生**：記分完成後按「下一場」才依當下在場人員即時計算下一場分組；可顯示「下一場預告」（僅預告，人員變動時可能改變），不預排整晚
- 公平性指標＝**當日上場次數**：安排休息時挑上場次數最多者，並列時隨機；遲到者上場次數少、會連打數場直到追平，此為預期行為
- 自願休息：被標記者該回合跳過安排
- 兩隊強度盡量平衡（以 rating 計算），但輪替公平性優先於強度平衡
- 單打／雙打：每回合皆可切換，預設雙打；單打畫面為左右各一大區塊、中間 VS
- **手動調組**：自動分組後，在分組預覽畫面「點選兩人交換」（含與休息區的人交換）；統計與 rating 一律以實際確認開打的分組計算

## 計分與 rating

- 賽後一次輸入比分：兩個非負整數，不可平手，不強制 21 分制校驗（支援 15 分制、計時制等），勝負以大小判定；不做即時計分板
- **Glicko-2**：
  - rating period ＝ 一場比賽，每場打完即時更新
  - 雙打映射：每位球員以「對方兩人 rating 平均」為虛擬對手更新自己的 rating；只看勝負、不看分差
  - 單打為標準一對一更新；單、雙打共用同一 rating
  - 新人初始值：選初始等級（新手／中等／較強，對應不同初始 rating），RD 350、volatility 0.06
  - 保留手動覆寫 rating 入口；覆寫時將 RD 重設為高值（視為重新認識此人）
- 歷史紀錄頁：依場次分群列出每場（時間、對戰組合、比分）；可修改／刪除任一場，改動後**從完整歷史全量重算**所有人的 rating（保證 rating 與紀錄永遠一致，資料量小、瞬間完成）

## 其他

- 顏色：新增參賽者時自動指派預設色；開放任意 color picker 自訂；依底色亮度自動配黑或白的文字色確保可讀
- 參賽者列表欄位：名字、強度分數、比賽次數、休息次數、代表顏色
- 記分輸入等操作有誤時可事後於歷史頁修正（見上）
