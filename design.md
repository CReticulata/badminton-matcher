# 羽球雙打隊伍產生器 — 設計決策文件

本文件記錄 2026-08-05 grilling session 定案的所有設計決策，為 `spec.md` 的補充與細化。實作以本文件為準。

## 產品形態

- 技術棧：Vue 3 + TypeScript + Vite + Tailwind CSS，套件管理用 pnpm
- 純前端、無後端無資料庫；資料以 JSON 存於 localStorage
- 最簡 PWA（manifest + service worker 快取靜態資源），支援離線使用（體育館收訊差為真實場景）
- 部署：Cloudflare Pages，正式網域 `badminton-matcher.creticulata.dev`；部署指令執行前需經使用者確認
- 介面語言：繁體中文
- 主題：操作頁面（名單、記分、歷史、設定）為淺色主題、直式為主；對戰顯示畫面為高對比深底大字
- 螢幕方向：不鎖定方向。對戰畫面橫式為四分割（左二區塊一隊、右二區塊一隊、中間 VS）；直式時改為上下堆疊佈局並顯示「建議轉為橫式」提示

## 資料模型

- **場次（session）制**：全域參賽者名單與 rating 永久保留、跨場次累積；「今日出席名單、上場／休息統計、當日賽程」屬於單一場次，開新場次時重置
- 開新場次時從全域名單勾選今日出席者；中途可加入（遲到）或標記離場（早退）
- CSV：單一檔案匯出／匯入，內含參賽者與比賽紀錄兩個區段（如 `[players]` / `[matches]`）；匯入為**覆蓋還原**語意（換手機／備份用途），匯入前跳確認

## 賽程與分組

- 單場地：一回合一場比賽（架構保留未來多場地擴充，第一版不做 UI）
- **逐回合產生**：記分完成後按「下一場」才依當下在場人員即時計算下一場分組，不預排整晚
- 公平排序依序為：**當日上場次數較少者優先 → 次數相同時連續上場場數較少者優先 → 完全相同時隨機**。遲到者仍會先連打直到上場次數追平
- 連續上場場數從同一場次最新一場已完成且有比分的比賽向前計算；只看最終實際分組，休息或未上場即中斷，單打與雙打共用紀錄，開新場次時重置
- 自願休息：被標記者該回合跳過安排
- 兩隊強度盡量平衡（以 rating 計算），但輪替公平性優先於強度平衡
- 單打／雙打：每回合皆可切換，預設雙打；單打畫面為左右各一大區塊、中間 VS
- **手動調組**：自動分組後，在分組預覽畫面「點選兩人交換」（含與休息區的人交換）；統計與 rating 一律以實際確認開打的分組計算

## 計分與 rating

- 賽後一次輸入比分：兩個非負整數，不可平手，不強制 21 分制校驗（支援 15 分制、計時制等），勝負以大小判定；不做即時計分板
- **Glicko-2**：
  - rating period ＝ 一場比賽，每場打完即時更新
  - 雙打映射：每位球員以「對方兩人 rating 平均」為虛擬對手更新自己的 rating
  - 觀測得分：賽制已知時由比分反推每球勝率再換算成勝率餵給 Glicko（15:13 給約 0.66）；賽制未知時退回 1／0
  - 單打為標準一對一更新；單、雙打共用同一 rating
  - **級數＝mu + 8**：級數就是 Glicko-2 內部量 mu 平移 8 級，1 級 = 1 mu = 173.7178 rating，8 級 = 1500。選這個刻度是因為 mu 才是 logistic 的自然單位（一級差＝勝算 e 倍），而非在 rating 尺度上人為挑一個間距。一級差的勝率：對手 RD 350 時 66.1%、RD 50 時 72.9%
  - 新人初始值：依台灣羽球推廣協會分級選 1–18 級（新手階至職業級）；初始 rating 由級數換算（1 級 284、8 級 1500、18 級 3237），RD 350、volatility 0.06。選單只呈現級數與階名，不顯示換算出的 rating——級數是選的人唯一需要判斷的東西
  - 顯示單位：參賽者頁與活動名單顯示「積分（級數）」兩者並列（如 `1600（8.6 級）`），歷史單場變動與活動摘要維持純 rating；rating 仍是唯一儲存與計算的量，換算在 `src/lib/level.ts`。超出 1–18 的 rating 顯示真實級數不夾範圍，包含負級數——夾了會讓排序中實力不同的兩人看起來一樣
  - 保留手動覆寫 rating 入口；覆寫時將 RD 重設為高值（視為重新認識此人）
- 歷史紀錄頁：依場次分群列出每場（時間、對戰組合、比分），並在實際上場者姓名上方顯示該場整數 rating 增減；已結束活動可手動展開開場、結束與整日差額摘要
- 可修改／刪除任一場；改動後從該活動固定保存的完整 Glicko 開場狀態重播該活動內該場與後續比賽，影響不得穿透下一活動的開場邊界。舊活動無法可靠回推開場狀態時保留紀錄，但隱藏不可信的 rating 變動與摘要
- 活動先後不可只依 timestamp 判定；`startedAt` 相同時以活動建立順序維持邊界。活動內 match／override／baseline 共用 replay timeline，同 timestamp 時依 match → override → baseline 套用

## 清除歷史紀錄（2026-08-06 新增）

- **兩種粒度**：歷史頁每個場次群組旁「清除此場次」；歷史頁最底部「清除全部歷史」（紅色文字按鈕、與場次清單保持距離避免誤觸）
- **rating 語意**：預設**保留目前強度**——清除前將**全體球員**當下的 rating／RD／volatility 完整固化為新基準（不沿用手動覆寫「重設 RD」的語意；必須全員固化，只固化參與者會讓其他場次交手過的第三方被連動改分）；之後全量重算以此基準起算。modal 內勾選框「同時重設相關球員的強度分數」可改為連戰績一起抹除（重算後 rating 退回沒有這些比賽時的狀態）
- **基準前結凍**：早於最新固化基準的比賽視為已結算——仍可修改／刪除以留存正確紀錄，但不再影響強度分數；歷史頁對這類比賽顯示「已結算」標記，編輯時提示「僅更新紀錄、不影響強度分數」（2026-08-06 審查後定案，取代原先「保留強度後仍可任意改早期比分」的隱含假設）
- **警告 modal**：自訂 modal（淺色主題），兩種清除粒度共用。內容：將刪除 N 場紀錄（N＝實際比賽紀錄筆數）、此動作無法復原、內附「先匯出 CSV 備份」按鈕、上述勾選框。不做輸入文字的高摩擦確認
- **場次處理**：已結束的場次清除紀錄時連場次一併刪除；**進行中的場次**只刪比賽紀錄、保留場次與出席名單，modal 額外警示「今日上場／休息統計將歸零，影響後續輪替安排」；「清除全部」同理（刪所有紀錄與已結束場次，進行中場次保留但統計歸零）
- 固化基準事件需納入 CSV 匯出／匯入與全量重算的重播序列，保證清除後 rating 仍與資料一致

## 其他

- 顏色：新增參賽者時自動指派預設色；開放任意 color picker 自訂；依底色亮度自動配黑或白的文字色確保可讀
- 參賽者列表欄位：名字、強度分數、比賽次數、休息次數、代表顏色
- 記分輸入等操作有誤時可事後於歷史頁修正（見上）
