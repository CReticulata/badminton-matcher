## 1. Establish a shared-worktree safety baseline

- [x] 1.1 Before editing implementation files, save complete `git status --porcelain=v1 -uall` output outside the repository and record SHA-256 values for every planned target: `src/types.ts`, `src/store.ts`, `src/store.test.ts`, `src/lib/csv.ts`, `src/lib/__tests__/csv.test.ts`, `src/lib/rating-j1/__tests__/shadow-noninterference.test.ts`, `vitest.browser.config.ts`, planned scoring-format/normalization modules and tests, affected Vue components/tests, and this change. Record pre-existing `package.json`, `pnpm-lock.yaml`, Glicko, matchmaking, J1 method/Worker, and shadow-adapter implementation hashes as protected non-targets.
- [x] 1.2 Inventory every `Session`, `Match`, `RoundProposal`, `ui.pending`, and `ui.live` construction site plus CSV/localStorage fixture. Record the required fixture migration list before making `scoringFormat` fields mandatory; do not attribute pre-existing dirty files to this apply.
- [x] 1.3 Confirm no design-impacting open question remains and record the authorized boundary: browser TypeScript/localStorage/CSV only; no dependency changes, backend, Python runtime, IndexedDB, simulation, score-aware rating, completed-format editing, promotion, or cutover.

## 2. Build the pure scoring-format contract with RED first

- [x] 2.1 Add `src/lib/__tests__/scoring-format.test.ts` with assertion-level RED cases for exact catalog/custom/unknown variants, defensive copying and attempted nested mutation through every constructor/reconstructor, the two versioned catalog entries, 1–40-code-point exact-Unicode custom labels, exact-field/type/schema rejection, safe-integer rule boundaries including `winBy <= target <= cap`, canonical serialization, display labels, team-swap symmetry, and the complete disjoint target/deuce/cap endpoint partition including `cap == target`.
- [x] 2.2 Run `npm exec --yes --package=pnpm@11.22.0 -- pnpm exec vitest run src/lib/__tests__/scoring-format.test.ts --reporter=verbose` and retain the expected RED output before creating `src/lib/scoring-format.ts`.
- [x] 2.3 Implement the minimal pure browser-safe schema, constructors, catalog, reconstruction validator, endpoint validator, canonical encoder, and label helpers in `src/lib/scoring-format.ts`; add public readonly snapshot/context types in `src/types.ts` without importing store, Glicko, matchmaking, UI, Worker, Node, or `analysis/` authority.
- [x] 2.4 Re-run the exact task 2.2 command and obtain GREEN, including adversarial extra-field, bool/numeric-string, unsafe-integer, catalog-rule mismatch, mutation/alias, and unsupported-schema cases.

## 3. Normalize local data and protect migration before enabling writes

- [x] 3.1 Add `src/lib/__tests__/app-data-normalization.test.ts` with RED cases proving truly absent legacy session/match fields become detached `legacy-missing` unknown snapshots; score resemblance, dates, names, and later defaults never infer a format; malformed present snapshots and known-format illegal endpoints reject the whole value; and valid mixed variants round-trip without aliasing.
- [x] 3.2 Add focused RED store/storage cases in `src/store.test.ts` proving an existing `badminton-matcher:v1` value is backed up once to `badminton-matcher:pre-scoring-format-v1` and verified byte-for-byte before the first enriched write; an absent active key needs no backup; existing backup is never overwritten; quota/write/readback failure and malformed explicit local data preserve active raw storage, enter `blocked`, suspend all writes/mutating commands, and never catch-to-empty overwrite; cancelled recovery stays blocked; raw download and confirmed discard preserve the specified boundary.
- [x] 3.3 Run `npm exec --yes --package=pnpm@11.22.0 -- pnpm exec vitest run src/lib/__tests__/app-data-normalization.test.ts src/store.test.ts --reporter=verbose` and retain the intended normalization/storage RED failures before implementation.
- [x] 3.4 Implement exact AppData normalization in a pure `src/lib/app-data-normalization.ts` boundary and the store-owned `ready`/`blocked` load, raw-download, confirmed-discard, exact backup verification, and write-gating lifecycle in `src/store.ts`; do not connect CSV recovery until task 4.6. Migrate the task 1.2 fixture/call-site inventory to explicit snapshot types without changing Glicko or matchmaking semantics.
- [x] 3.5 Re-run the exact task 3.3 command and obtain GREEN; verify local corruption leaves the original and backup values intact and blocks automatic writes until explicit recovery.

## 4. Add lossless, backward-compatible CSV semantics

- [x] 4.1 Extend `src/lib/__tests__/csv.test.ts` first with RED cases for canonical `defaultScoringFormat` and `scoringFormat` JSON cells; catalog/custom/unknown mixed round-trip; quoted custom labels; absent legacy columns becoming unknown; exact preservation of existing fields; legacy-header projection consuming new CSV while ignoring format columns and re-upgrade classifying stripped provenance as unknown; unique known sections/headers and exact known-row widths; 5 MiB UTF-8, 50,000-record, and 64 KiB decoded-field limits with early abort; and atomic rejection of duplicate format headers, surplus/missing cells, known-format illegal endpoints, empty/partial snapshots, extra snapshot fields, contradictory variants, unsafe integers, catalog mismatch, and unsupported schema.
- [x] 4.2 Run `npm exec --yes --package=pnpm@11.22.0 -- pnpm exec vitest run src/lib/__tests__/csv.test.ts --reporter=verbose` and retain the expected RED output before editing `src/lib/csv.ts`.
- [x] 4.3 Add the two atomic canonical-JSON columns, reconstruction validation, and text/parser-level UTF-8/record/field budgets to `src/lib/csv.ts`. Parse and normalize the complete candidate AppData before `importCsvText()` assigns any reactive array; missing columns alone use the legacy-unknown path.
- [x] 4.4 Re-run the exact task 4.2 command and obtain GREEN, then prove `importCsvText()` leaves existing application data byte-equivalent after any malformed explicit snapshot failure.
- [x] 4.5 Add a RED store test for blocked-state CSV recovery: invalid or over-budget CSV leaves active raw data and state unchanged; valid CSV is fully parsed/normalized and written before reactive assignment and transition to `ready`.
- [x] 4.6 Run `npm exec --yes --package=pnpm@11.22.0 -- pnpm exec vitest run src/lib/__tests__/csv.test.ts src/store.test.ts --reporter=verbose`, retain the intended recovery RED assertion, then minimally connect `recoverFromCsvText()` to the completed CSV parser and store lifecycle.
- [x] 4.7 Re-run the exact task 4.6 command and obtain GREEN with exact ordering and failure-state assertions.

## 5. Freeze format before outcome and prove authority non-interference

- [x] 5.1 Add RED store cases in `src/store.test.ts` for explicit session choice; a `legacy-missing` active session blocking its next match until replaced by catalog/custom/`explicit-unknown`; cancelled choice remaining blocked; pre-start inherited/override selection; detached pending/live/completed copies; prospective default changes; no override after start; known legal/illegal submission; preservation of current `Number.isInteger` unknown submission; unchanged snapshot on score edit; and rejection-before-history/rating/UI mutation.
- [x] 5.2 Add RED non-interference cases to `src/lib/rating-j1/__tests__/shadow-noninterference.test.ts` that compare accepted catalog/custom/unknown matches with the same winner and assert identical official Glicko state and matchmaking proposals; assert an illegal known endpoint produces no persisted match, rating change, preparation-token outcome, or shadow authority effect.
- [x] 5.3 Run `npm exec --yes --package=pnpm@11.22.0 -- pnpm exec vitest run src/store.test.ts src/lib/rating-j1/__tests__/shadow-noninterference.test.ts --reporter=verbose` and retain the intended RED output before changing store flow.
- [x] 5.4 Introduce separate pending/live match context in `src/types.ts` and `src/store.ts`; leave `RoundProposal`, `src/lib/glicko2.ts`, and `src/lib/matchmaking.ts` unchanged. Freeze a detached snapshot in `startMatch()` and validate it upstream of every `submitScore()`/`editMatchScore()` effect.
- [x] 5.5 Re-run the exact task 5.3 command and obtain GREEN, including exact no-side-effect assertions for every validation failure and identity of winner-only authority outputs across format variants.

## 6. Add explicit, compact UI surfaces with real-browser TDD

- [x] 6.1 Add a focused real-browser RED test under `src/components/__tests__/scoring-format-browser.test.ts` and minimally extend `vitest.browser.config.ts` so that test is collected. The RED test must mount the relevant Vue flow and prove: session start has no silent catalog preselection; catalog/custom/unknown are explicit choices; custom draft validation and Save/Cancel semantics; a migrated active session blocks next-match start until a deliberate choice and remains blocked on cancel; pre-start override does not change the session default; live score UI displays but cannot edit the frozen format; history distinguishes catalog/custom/unknown; `File.size` rejects an over-5-MiB import before `.text()`; blocked-storage raw download, failed/over-budget recovery, valid CSV recovery, confirmed discard, and cancellation preserve the specified lifecycle; blocking entry focus, labels, `aria-describedby`/live errors, first-invalid focus, trigger-focus restoration, Escape behavior, absent background actions, 44px targets, and a no-horizontal-scroll 320px layout all hold.
- [x] 6.2 Run `npm exec --yes --package=pnpm@11.22.0 -- pnpm exec vitest --config vitest.browser.config.ts run src/components/__tests__/scoring-format-browser.test.ts --reporter=verbose` in real Chromium and retain assertion failures from the expected RED test before editing product UI components; a no-tests-collected result is not RED evidence. Assertion-level RED saved at `/tmp/badminton-scoring-format-apply/6.2-host-complete-red.log`.
- [x] 6.3 Implement the minimal draft-based format picker/display/recovery surfaces in a reusable component plus `SessionView.vue`, `PreviewView.vue`, `MatchDisplay.vue`, `ScoreInput.vue`, `HistoryView.vue`, and the app shell as required. Include inline custom-field errors, explicit Save/Cancel, legacy one-time blocking choice, and the store-owned recovery actions; keep controls outside score submission and expose no completed-format editor or speculative score-aware output.
- [x] 6.4 Re-run the exact task 6.2 command and obtain real-Chromium GREEN with no skips; report the accessibility, focus, unknown/custom labeling, touch-target, and 320px assertions explicitly without adding dependencies. Chromium GREEN (7 passed, 0 skipped) saved at `/tmp/badminton-scoring-format-apply/6.4-host-complete-green.log`.

## 7. Focused closure, broader verification, and owner gate

- [x] 7.1 Run the focused Node closure: `npm exec --yes --package=pnpm@11.22.0 -- pnpm exec vitest run src/lib/__tests__/scoring-format.test.ts src/lib/__tests__/app-data-normalization.test.ts src/lib/__tests__/csv.test.ts src/store.test.ts src/lib/rating-j1/__tests__/shadow-noninterference.test.ts --reporter=verbose`.
- [x] 7.2 Run the full existing Node suite with `npm exec --yes --package=pnpm@11.22.0 -- pnpm test`, then the complete real Chromium gate with `npm exec --yes --package=pnpm@11.22.0 -- pnpm exec vitest --config vitest.browser.config.ts run --reporter=verbose`; report exact pass/fail/skip counts without calling any skipped or unrun surface GREEN.
- [x] 7.3 Run `npm exec --yes --package=pnpm@11.22.0 -- pnpm exec vue-tsc --noEmit --pretty false` and `npm exec --yes --package=pnpm@11.22.0 -- pnpm build`; verify no new production dependency, backend, Python import, IndexedDB path, score-aware calculation, or format-to-rating branch exists.
- [x] 7.4 Run `openspec validate define-scoring-format-snapshot-semantics --strict --no-interactive` and `git diff --check` for tracked targets. Directly inspect every owned untracked file for trailing whitespace, merge markers, and final newline because Git does not cover untracked contents.
- [x] 7.5 Compare complete post-edit status and target/protected hashes with task 1.1. Require unchanged `package.json`, `pnpm-lock.yaml`, Glicko, matchmaking, J1 method/Worker, archived research/protocol, and stable roadmap spec; report concurrent unrelated changes without claiming them.
- [x] 7.6 Replace the pending section below with a durable receipt containing RED/GREEN commands and real outputs, browser identity, revision/worktree scope, pre/post hashes, owned paths, limitations including unsupported full-fidelity downgrade through an older build, and explicit statuses for research evidence, engineering evidence, formal inference, safety evaluation, protocol advancement, and production authorization.
- [x] 7.7 Stop for owner review. Do not archive, commit, push, run simulations, create a candidate-study change, or begin any format-correction/rating-authority work without separate explicit approval.

## Verification receipt

Implementation and review are complete under the owner's explicit apply and shipping approvals. The durable command-level receipt is `verification.md` in this archived change directory.

- Focused review closure: 3 Node files / 28 tests and one deterministic Chromium race test passed after assertion-level RED evidence.
- Final Node: 19 files / 112 tests passed.
- Final real-browser gate: Vitest Browser Mode with Playwright Chromium, 2 files / 11 tests passed / 0 skipped.
- Typecheck, production build, strict OpenSpec validation, and `git diff --check`: passed.
- Protected baseline: 33,292 of 33,293 paths byte-identical; the sole declared exception is the test-only J1 Worker browser fixture updated with the newly mandatory snapshot.
- Engineering evidence: GREEN for the bounded commands in `verification.md`; research outcomes NOT RUN; formal inference false; safety NOT_EVALUATED; protocol advancement false.
- Production authorization remains limited to scoring-format provenance/persistence/UI. Glicko remains the sole rating and matchmaking authority.
