# 羽球雙打隊伍產生器

目的是解決打羽球時需要人工分組的困擾，且因為每次打球人數不同，需要依照人數安排不同的分組方式，以及需要考慮每個人的強度不同，並在分組後還需要人工呼喊，無法讓參與者自行觀看螢幕得知分組狀況的情形。

## 需求

1. 可以有雙打和單打，預設為雙打
2. 可以輸入參賽者的名字（要可以更改），並提供每個人不同的顏色（底色），顏色可以讓參賽者自行設定
3. 使用時以手機橫式螢幕為主，顯示當前分組時需要切分手機橫式螢幕成四個區塊，左邊上下兩個區塊代表一組，右邊上下兩個區塊代表另一組，中間顯示 VS 表示「對上」的意思
4. 比賽結束後可以填寫兩組的得分，並紀錄下來
5. 可以填寫每個參賽者的強度，並依照多次比賽的歷史紀錄調整此人的強度，請參考 glicko-2 計算方法來衡量大家的強度分數
6. 需要可以將紀錄存成 csv 並匯出，同理也可以將 csv 匯入
7. 由於每次打球人數都不同，也會因為有人遲到或早退而有不同，因此需要依照不同人數而有不同的分組方法，例如如果是 5 個人，每回合就需要安排 1 個人休息，需要讓每個人的休息次數盡量是一樣的，也就是如果這次還沒有這 5 個人都休息過，那就不可能有人會休息 2 次，也就是追求公平的上場次數，除非是這個人自願要休息（參照第 8 點）；如果是 8 個人，也不能都是同樣 4 個人在比賽，也就是需要輪替
8. 需要有可以自願休息的設定，這回合就會跳過安排這個人
9. 分組時兩隊的強度要盡量相同，但第 7 點優先
10. 參賽者列表至少要可以看到：名字、強度分數、比賽次數、休息次數、代表顏色
11. 自動分組後，仍然可以手動安排分組，並依照手動安排的分組計算數據
12. 有疑慮的地方，請先問我

## 技術棧

1. 前端使用 Vue.js 3
2. 不使用後端、不串接資料庫
3. 數據可以暫存瀏覽器
4. 使用 pnpm
